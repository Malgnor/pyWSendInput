from setuptools import setup

setup(
    name='PyWSendInput',
    author='Malgnor',
    author_email='',
    url='https://github.com/aamlima/pyWSendInput',
    version='0.1',
    packages=['PyWSendInput'],
    description='A python3 windows-only module for keyboard/mouse input and events.'
)
